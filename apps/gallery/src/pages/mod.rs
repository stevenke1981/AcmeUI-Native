//! Category page modules — each exposes `impl crate::Gallery` with page builder methods.

mod accessibility;
mod component;
mod data;
mod foundations;
mod inputs;
mod navigation;
mod overlay;
mod patterns;
mod stress;
