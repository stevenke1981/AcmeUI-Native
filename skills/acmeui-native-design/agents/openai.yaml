interface:
  display_name: "AcmeUI Native Design"
  short_description: "Design and validate AcmeUI and cross-stack interfaces"
  default_prompt: "Use $acmeui-native-design to route, design, and validate this UI task while preserving the target stack."
policy:
  allow_implicit_invocation: true
